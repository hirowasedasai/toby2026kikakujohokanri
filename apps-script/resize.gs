// Keep the existing callable entry point; source answers and sharing stay unchanged.
function resizeAHColumnImagesToSpecificFolder() {
  return processParticipantImages_(true);
}

function extractFileIdFromUrl_(url) {
  return extractFileId(url);
}
